# Mental Smile Commercial Capsule

Materialized Commercial capsule package for the `MS-PORT-COM-TEST-01` test port.

This package is local-only. It exposes a public entry, descriptors, contracts,
and test diagnostics. Production Firebase, Storage, auth mutation, and active
Commercial runtime imports are intentionally absent.
